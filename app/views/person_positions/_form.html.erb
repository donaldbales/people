<%= form_for(@person_position) do |f| %>
  <% if @person_position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@person_position.errors.count, "error") %> prohibited this person_position from being saved:</h2>

      <ul>
      <% @person_position.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :person_id %><br>
    <%= f.number_field :person_id %>
  </div>
  <div class="field">
    <%= f.label :position_id %><br>
    <%= f.number_field :position_id %>
  </div>
  <div class="field">
    <%= f.label :active_datetime %><br>
    <%= f.datetime_select :active_datetime %>
  </div>
  <div class="field">
    <%= f.label :inactive_datetime %><br>
    <%= f.datetime_select :inactive_datetime %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
