<p id="notice"><%= notice %></p>

<h1>Listing Person Types</h1>

<table>
  <thead>
    <tr>
      <th>Code</th>
      <th>Description</th>
      <th>Active datetime</th>
      <th>Inactive datetime</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @person_types.each do |person_type| %>
      <tr>
        <td><%= person_type.code %></td>
        <td><%= person_type.description %></td>
        <td><%= person_type.active_datetime %></td>
        <td><%= person_type.inactive_datetime %></td>
        <td><%= link_to 'Show', person_type %></td>
        <td><%= link_to 'Edit', edit_person_type_path(person_type) %></td>
        <td><%= link_to 'Destroy', person_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Person type', new_person_type_path %>
