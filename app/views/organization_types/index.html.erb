<p id="notice"><%= notice %></p>

<h1>Listing Organization Types</h1>

<table>
  <thead>
    <tr>
      <th>Code</th>
      <th>Description</th>
      <th>Active datetime</th>
      <th>Inactive datetime</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @organization_types.each do |organization_type| %>
      <tr>
        <td><%= organization_type.code %></td>
        <td><%= organization_type.description %></td>
        <td><%= organization_type.active_datetime %></td>
        <td><%= organization_type.inactive_datetime %></td>
        <td><%= link_to 'Show', organization_type %></td>
        <td><%= link_to 'Edit', edit_organization_type_path(organization_type) %></td>
        <td><%= link_to 'Destroy', organization_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Organization type', new_organization_type_path %>
